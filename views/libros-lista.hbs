<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Libros</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h1>Lista de Libros</h1>
        <table>
            <thead>
                <tr>
                    <th>Identificación</th>
                    <th>Título</th>
                    <th>Año Publicación</th>
                    <th>Género</th>
                    <th>Autor</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {{#each libros}}
                <tr>
                    <td>{{this.identificacion}}</td>
                    <td>{{this.titulo}}</td>
                    <td>{{this.año_publicacion}}</td>
                    <td>{{this.genero}}</td>
                    <td>{{this.id_autor.nombre}}</td>
                    <td>
                        <form action="/libros/editar/{{this._id}}" method="get">
                            <button type="submit">Editar</button>
                        </form>
                        <form action="/libros/eliminar/{{this._id}}" method="post">
                            <button type="submit">Eliminar</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        <a href="/libros/nuevo" class="button">Añadir Nuevo Libro</a>
                    <a href="/" class="button">Ir al Inicio</a>
    </div>
</body>
</html>


