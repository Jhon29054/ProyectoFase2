<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario</title>
    <link rel="stylesheet" href="/css/styles.css"> 
</head>
<body>
    <div class="container">
<h1>Editar Libro</h1>
<form action="/libros/editar/{{libro._id}}" method="POST">
    <label for="identificacion">Identificación:</label>
    <input type="number" name="identificacion" value="{{libro.identificacion}}" required>

    <label for="titulo">Título:</label>
    <input type="text" name="titulo" value="{{libro.titulo}}" required>

    <label for="año_publicacion">Año de Publicación:</label>
    <input type="number" name="año_publicacion" value="{{libro.año_publicacion}}" required>

    <label for="genero">Género:</label>
    <input type="text" name="genero" value="{{libro.genero}}" required>

    <label for="id_autor">Autor:</label>
    <select name="id_autor" required>
        <option value="">Seleccionar autor</option>
        {{#each autores}}
            <option value="{{this._id}}" {{#if (eq this._id ../libro.id_autor)}}selected{{/if}}>{{this.nombre}}</option>
        {{/each}}
    </select>

    <button type="submit">Guardar Cambios</button>
</form>
            <a href="/" class="button">Ir al Inicio</a>



