<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Miembros</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h1>Lista de Miembros</h1>
        <table>
            <thead>
                <tr>
                    <th>Identificación</th>
                    <th>Nombre</th>
                    <th>Fecha de Afiliación</th>
                    <th>Correo Electrónico</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {{#each miembros}}
                <tr>
                    <td>{{this.identificacion}}</td>
                    <td>{{this.nombre}}</td>
                    <td>{{this.fecha_afiliacion}}</td>
                    <td>{{this.correo_electronico}}</td>
                    <td>
                        <form action="/miembros/editar/{{this._id}}" method="get">
                            <button type="submit">Editar</button>
                        </form>
                        <form action="/miembros/eliminar/{{this._id}}" method="post">
                            <button type="submit">Eliminar</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        <a href="/miembros/nuevo" class="button">Añadir Nuevo Miembro</a>
                    <a href="/" class="button">Ir al Inicio</a>
    </div>
</body>
</html>
